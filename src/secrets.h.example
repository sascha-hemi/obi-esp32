/**
 * WiFi Access Point Credentials
 *
 * Copy this file to secrets.h to override the default AP settings.
 * secrets.h is gitignored and will not be committed.
 *
 * The ESP32-S3 will broadcast its own WiFi network.
 * Connect to it and open http://192.168.4.1 in a browser.
 */

#define AP_SSID "OBI-Battery"
#define AP_PASS ""  // Leave empty for an open (no password) network
